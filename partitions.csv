# Name,     Type, SubType,  Offset,     Size,      Flags
# ---------------------------------------------------------------------
# Total flash assumed: 8 MB (0x800000)
nvs,         data, nvs,      0x9000,     0x6000
phy_init,    data, phy,      0xf000,     0x1000
factory,     app,  factory,  0x10000,    0x180000
# Raw, unformatted data partition used directly via esp_partition_read/
# write/erase. subtype 0x40 is an arbitrary "custom" data subtype so the
# partition is never picked up by any filesystem component (FAT/LittleFS/
# NVS) that might try to mount or reformat it.
imu_log,     data, 0x40,     0x200000,   0x400000